apiVersion: v1
kind: Service
metadata:
  name: apuestas-service
  labels:
    app: apuestas-service
spec:
  # ClusterIP: http://apuestas-service:8005
  type: ClusterIP
  selector:
    app: apuestas-service
  ports:
    - name: http
      protocol: TCP
      port: 8005
      targetPort: 8005
